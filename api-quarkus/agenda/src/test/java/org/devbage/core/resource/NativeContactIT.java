package org.devbage.core.resource;

import io.quarkus.test.junit.NativeImageTest;

@NativeImageTest
public class NativeContactIT extends ContactTest {

    // Execute the same tests but in native mode.
}